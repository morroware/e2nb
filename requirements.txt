# E2NB - Email to Notification Blaster
# Required dependencies

# Twilio SDK for SMS, Voice Calls, and WhatsApp
twilio>=8.0.0

# Slack SDK for Slack notifications
slack_sdk>=3.20.0

# HTTP library for Telegram, Discord, and webhooks
requests>=2.28.0

# Optional: urllib3 for retry logic (usually installed with requests)
urllib3>=2.0.0

# RSS/Atom feed parsing (for RSS monitoring)
feedparser>=6.0.0

# HTML parsing (for web page monitoring and RSS content extraction)
beautifulsoup4>=4.12.0

# SMTP receiver for receiving emails via SMTP (alternative to IMAP)
aiosmtpd>=1.4.0
